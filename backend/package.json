{
  "name": "backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node scripts/seed.js",
    "seed:services": "node -e \"require('./scripts/seed').seedServices().then(() => process.exit(0))\"",
    "seed:styles": "node -e \"require('./scripts/seed').seedHaircutStyles().then(() => process.exit(0))\"",
    "seed:admin": "node -e \"require('./scripts/seed').seedAdminUsers().then(() => process.exit(0))\"",
    "create-admin": "node scripts/create-admin.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "description": "",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "moment": "^2.30.1",
    "mongoose": "^8.18.0",
    "twilio": "^5.9.0"
  }
}
